function exibir(elemento){
    alert(elemento.innerText);
}
const listaTeste = document.querySelectorAll('#lista')
for (let i = 0; i < lista.length; i++) {
    list[i].addEventListener('click',function(){exibir(this);});
    
}